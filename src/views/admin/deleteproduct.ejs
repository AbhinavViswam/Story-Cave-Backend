<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Delete</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #f6f5f7; /* Light background to match the update page */
            font-family: 'Montserrat', sans-serif;
            color: #333;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .container {
            background: #fff;
            border-radius: 8px;
            padding: 30px 25px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            text-align: center;
            width: 100%;
            max-width: 400px; /* Adjusted max-width for the container */
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #333;
        }

        p {
            margin: 10px 0;
            font-size: 1rem;
            color: #333;
        }

        button {
            background-color: #ff00005b;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 5px;
            margin: 10px;
            transition: background 0.3s ease;
        }

        button:hover {
            background-color: #ff512f; /* Slightly lighter color for hover */
        }

        a {
            text-decoration: none;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Are you sure you want to delete this product?</h1>
        <p><strong>Product Name:</strong> <%= product.name %></p>
        <p><strong>Category:</strong> <%= product.category.name %></p>

        <!-- Form to confirm deletion -->
        <form action="/admin/delete-product/<%= pdct_id %>/confirm" method="POST">
            <button type="submit">Yes, Delete</button>
        </form>

        <!-- Cancel button -->
        <a href="/admin/view-product"><button>No, Go Back</button></a>
    </div>
</body>
</html>
