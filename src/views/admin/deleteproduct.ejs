<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Delete</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background-color: #080710;
            color: white;
            text-align: center;
        }
        .container {
            margin-top: 50px;
        }
        button {
            background-color: #ff00005b;
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 16px;
            cursor: pointer;
            border-radius: 4px;
            margin: 10px;
        }
        button:hover {
            background-color: #ff512f;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Are you sure you want to delete this product?</h1>
        <p><strong>Product Name:</strong> <%= product.name %></p>
        <p><strong>Category:</strong> <%= product.category.name %></p>

        <!-- Form to confirm deletion -->
        <form action="/admin/delete-product/<%= pdct_id %>/confirm" method="POST">
            <button type="submit">Yes, Delete</button>
        </form>

        <!-- Cancel button -->
        <a href="/admin/view-product"><button>No, Go Back</button></a>
    </div>
</body>
</html>
