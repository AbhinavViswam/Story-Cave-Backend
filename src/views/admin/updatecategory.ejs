<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Category</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;600&display=swap" rel="stylesheet">
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            background-color: #f6f5f7; /* Light background */
            font-family: 'Montserrat', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
            padding: 20px;
        }

        .container {
            background: #fff;
            border-radius: 8px;
            padding: 30px 25px;
            box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px; /* Adjusted max-width for the container */
            text-align: center; /* Center text inside the container */
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 20px;
            color: #333;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ddd;
            border-radius: 5px;
            margin-bottom: 15px;
            font-size: 1rem;
        }

        input[type="submit"] {
            background-color: #ff5722; /* Button color */
            color: white;
            border: none;
            padding: 10px 20px;
            font-size: 1rem;
            cursor: pointer;
            border-radius: 5px;
            transition: background 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #ff8a3d; /* Button hover color */
        }

        .error-message, .success-message {
            margin-top: 15px;
            padding: 10px;
            border-radius: 5px;
            font-size: 0.875rem;
            text-align: center;
        }

        .error-message {
            color: #ff4f4f;
            background-color: rgba(255, 79, 79, 0.1);
        }

        .success-message {
            color: #4caf50;
            background-color: rgba(76, 175, 80, 0.1);
        }

        a {
            display: inline-block;
            margin-top: 20px;
            text-decoration: none;
            color: #00294d;
            font-weight: 600;
            padding: 10px;
            border-radius: 5px;
            transition: background 0.3s ease;
            background: rgba(255, 255, 255, 0.07);
        }

        a:hover {
            background: linear-gradient(135deg, #ff512f, #dd2476);
            color: #fff;
        }

        /* Media Queries */
        @media (max-width: 480px) {
            .container {
                padding: 20px 15px;
            }

            h1 {
                font-size: 1.25rem;
            }

            input[type="text"], input[type="submit"] {
                font-size: 0.875rem;
                padding: 8px;
            }

            .error-message, .success-message {
                font-size: 0.75rem;
                padding: 8px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Update Category</h1>
        <form action="/admin/update-category/<%= categoryid %>" method="post">
            <input type="text" name="categoryName" value="<%= categoryName %>" placeholder="Category Name" required>
            <input type="submit" value="Update Category">
        </form>
        
        <% if (error) { %>
            <div class="error-message">
                <%= error %>
            </div>
        <% } %>

        <% if (message) { %>
            <div class="success-message">
                <%= message %>
            </div>
        <% } %>
        
        <a href="/admin/add-category">Go Back</a>
    </div>
</body>
</html>