<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Link to your main CSS file -->
    <style>
        /* Inline CSS for quick customization */
        body {
            font-family: 'Montserrat', sans-serif;
            background-color: #f6f5f7;
            display: flex;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            color: #333;
        }

        .product-container {
            background-color: #fff;
            width: 90%;
            max-width: 500px;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .product-image img {
            height: 250px;
            width: 200px;
            object-fit: cover;
            border-radius: 8px;
        }

        .product-details {
            margin-top: 20px;
        }

        .product-details h1 {
            font-size: 24px;
            margin-bottom: 10px;
            color: #333;
        }

        .product-details p {
            margin: 5px 0;
        }

        .price {
            font-size: 20px;
            font-weight: bold;
            color: #27ae60;
            margin: 10px 0;
        }

        .original-price {
            text-decoration: line-through;
            color: #888;
            font-size: 16px;
        }

        .offer-price {
            color: #e74c3c;
            font-size: 20px;
        }

        .product-meta {
            font-size: 14px;
            color: #666;
        }
    </style>
</head>
<body>
    <div class="product-container">
        <!-- Product Image -->
        <div class="product-image">
            <img src="<%= product.image %>" alt="<%= product.name %> image">
        </div>
        
        <!-- Product Details -->
        <div class="product-details">
            <h1><%= product.name %></h1>
            <p><%= product.description %></p>
            
            <!-- Meta information -->
            <p class="product-meta">Author: <%= product.author %></p>
            <p class="product-meta">Language: <%= product.language %></p>
            <p class="product-meta">Category: <%= product.category.name %></p>
            
            <!-- Pricing -->
            <div class="price-section">
                <% if (product.offerprice < product.price) { %>
                    <p class="original-price">$<%= product.price.toFixed(2) %></p>
                    <p class="offer-price">$<%= product.offerprice.toFixed(2) %></p>
                <% } else { %>
                    <p class="price">$<%= product.price.toFixed(2) %></p>
                <% } %>
            </div>
        </div>
        <div>
            <form action="/users/cart/<%= product._id %>" method="post">
                <input type="number" name="inputQuantity" min="1" value="1">
                <button>Add To Cart</button>
            </form>
        </div>
        <div>
            <form action="/users/wishlist/<%= product._id %>" method="post">
                <button>Add To wishlist</button>
            </form>
        </div>
        <a href="/users/main">All Products</a>
    </div>
</body>
</html>
