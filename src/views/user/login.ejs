<!DOCTYPE html>
<html lang="en">
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;600&display=swap" rel="stylesheet">
    <style media="screen">
      * {
          padding: 0;
          margin: 0;
          box-sizing: border-box;
      }
      body {
          background-color: #080710;
          font-family: 'Poppins', sans-serif;
          overflow: hidden;
          display: flex;
          flex-direction: column;
          justify-content: center;
          align-items: center;
          height: 100vh;
          position: relative;
      }

      .background {
          position: absolute;
          width: 100%;
          height: 100%;
          top: 0;
          left: 0;
      }

      /* Circle animations */
      .background .shape {
          height: 200px;
          width: 200px;
          position: absolute;
          border-radius: 50%;
          animation: float 6s ease-in-out infinite, breathing 4s ease-in-out infinite;
      }

      /* Position the circles closer to the form for laptop screens */
      .shape:first-child {
          background: linear-gradient(#1845ad, #23a2f6);
          left: 30%; /* Reduced left positioning to move closer to form */
          top: 20%;  /* Reduced top positioning */
        
      }
      .shape:last-child {
          background: linear-gradient(to right, #ff512f, #f09819);
          right: 30%; /* Reduced right positioning */
          bottom: 20%; /* Reduced bottom positioning */
      }


      /* Infinite breathing animation */
      @keyframes breathing {
          0%, 100% {
              transform: scale(1);
          }
          50% {
              transform: scale(1.05); /* Slightly increase size at mid-point */
          }
      }

      /* Floating up-and-down effect */
      @keyframes float {
          0% { transform: translateY(0px); }
          50% { transform: translateY(15px); }
          100% { transform: translateY(0px); }
      }

      /* Form styling */
      form {
          max-width: 400px;
          width: 90%;
          background-color: rgba(255, 255, 255, 0.13);
          border-radius: 10px;
          backdrop-filter: blur(10px);
          border: 2px solid rgba(255, 255, 255, 0.1);
          box-shadow: 0 0 40px rgba(8, 7, 16, 0.6);
          padding: 50px 35px;
          position: relative;
          z-index: 1;
      }

      form * {
          font-family: 'Poppins', sans-serif;
          color: #ffffff;
          letter-spacing: 0.5px;
          outline: none;
          border: none;
      }

      form h3 {
          font-size: 2rem;
          font-weight: 500;
          line-height: 2.5rem;
          text-align: center;
      }

      label {
          display: block;
          margin-top: 10px;
          font-size: 1rem;
          font-weight: 500;
      }

      input {
          display: block;
          height: 50px;
          width: 100%;
          background-color: rgba(255, 255, 255, 0.07);
          border-radius: 3px;
          padding: 0 10px;
          margin-top: 8px;
          font-size: 1rem;
          font-weight: 300;
          transition: background-color 0.3s ease;
      }

      input:focus {
          background-color: rgba(255, 255, 255, 0.1);
      }

      /* Attractive Button Styling */
      button {
          margin-top: 10px;
          width: 100%;
          background: linear-gradient(135deg, #ff512f, #dd2476);
          color: #fff;
          padding: 15px 0;
          font-size: 1.125rem;
          font-weight: 600;
          border-radius: 5px;
          cursor: pointer;
          box-shadow: 0 5px 10px rgba(255, 81, 47, 0.3);
          transition: all 0.4s ease;
      }

      button:hover {
          background: linear-gradient(135deg, #dd2476, #ff512f);
          box-shadow: 0 10px 15px rgba(255, 81, 47, 0.5);
          transform: translateY(-5px);
      }

      /* Error Message Styling */
      .error-message {
          margin-top: 10px;
          padding: 10px;
          color: #ff4f4f;
          background-color: rgba(255, 0, 0, 0.1);
          border: 1px solid #ff4f4f;
          border-radius: 5px;
          text-align: center;
          font-size: 0.875rem;
          visibility: hidden;
          height: auto;
          opacity: 0;
          transition: opacity 0.3s ease;
      }

      .error-message.active {
          visibility: visible;
          opacity: 1;
      }

      /* Social buttons */
      .social {
        margin-top: 15px;
        display: flex;
        justify-content: space-between;
      }

      .social a {
        width: 48%;
        text-align: center;
        padding: 10px 0;
        border-radius: 5px;
        background-color: #ffffff;
        color: #080710;
        font-size: 0.875rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.3s ease;
        box-shadow: 0 3px 6px rgba(0, 0, 0, 0.2);
        text-decoration: none;
        display: flex;
        justify-content: center;
        align-items: center;
      }

      .social a:hover {
        background-color: #f09819;
        color: #ffffff;
        transform: translateY(-2px);
        box-shadow: 0 5px 10px rgba(0, 0, 0, 0.3);
      }

      /* Media Queries for responsiveness */
      @media (max-width: 1024px) {
          .shape {
              height: 150px;
              width: 150px;
          }

          form h3 {
              font-size: 1.75rem;
          }

          label {
              font-size: 0.875rem;
          }

          input {
              font-size: 0.875rem;
          }

          button {
              font-size: 1rem;
          }
      }

      /* Media Queries for responsiveness */
@media (max-width: 768px) {
    .shape {
        height: 100px;  /* Smaller size */
        width: 100px;   /* Smaller size */
        /* Position them closer to the sides */
    }

    /* Position the first shape on the left */
    .shape:first-child {
        left: 5%; /* Adjust as needed */
        top: 15%;  /* Adjust top positioning */
    }

    /* Position the second shape on the right */
    .shape:last-child {
        right: 5%; /* Adjust as needed */
        bottom: 15%; /* Adjust bottom positioning */
    }

    form {
        width: 95%;
        padding: 40px 25px;
        box-shadow: 0 0 20px rgba(8, 7, 16, 0.5);
    }

    form h3 {
        font-size: 1.5rem;
    }

    label {
        font-size: 0.875rem;
    }

    input {
        height: 45px;
        font-size: 0.875rem;
    }

    button {
        padding: 12px 0;
        font-size: 0.875rem;
    }

    .social a {
        font-size: 0.75rem;
    }
}

@media (max-width: 480px) {
    .shape {
        height: 80px;  /* Smaller size */
        width: 80px;   /* Smaller size */
    }
    .titleheading{
            display: none;
          }
    /* Position the first shape on the left */
    .shape:first-child {
        left: 5%; /* Adjust as needed */
        top: 10%; /* Adjust top positioning */
    }

    /* Position the second shape on the right */
    .shape:last-child {
        right: 5%; /* Adjust as needed */
        bottom: 10%; /* Adjust bottom positioning */
    }

    form {
        padding: 30px 15px;
    }

    form h3 {
        font-size: 1.25rem;
    }

    button {
        font-size: 0.875rem;
        padding: 10px 0;
    }
}
.titleheading{
    color: white;
    font-family: 'poppins';
    font-size: 30px;
}
.X{
    font-size: 40px;
}

    </style>
    <title>Login</title>
</head>
<body>
    <div class="background">
        <div class="shape"></div>
        <div class="shape"></div>
    </div>
    <div class="titleheading">AMA<span class="X">X</span>ON</div>
    <form action="/users/login" method="post">
        <h3>Login</h3>
        <label for="email">Email</label>
        <input type="email" name="email" id="email" placeholder="Email" required>

        <label for="password">Password</label>
        <input type="password" name="password" id="password" placeholder="Password" required>

        <div class="error-message <%= error ? 'active' : '' %>"><%= error || '' %></div>

        <button type="submit">Log In</button>

        <div class="social">
          <a href="/users/register">Register here</a>
          <a href="/users/forgot-password">Forgot password?</a>
        </div>
    </form>
</body>
</html>