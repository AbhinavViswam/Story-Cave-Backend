<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Verify OTP</title>
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;800&display=swap" rel="stylesheet">
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
        }

        body {
            background: #f6f5f7; /* Changed to match the Password Reset page */
            font-family: 'Montserrat', sans-serif; /* Changed to match the Password Reset page */
            display: flex;
            justify-content: center;
            align-items: center;
            flex-direction: column;
            height: 100vh;
        }

        /* Form styling */
        form {
            max-width: 400px;
            width: 90%;
            background-color: #FFFFFF; /* Changed to match the Password Reset page */
            border-radius: 10px;
            box-shadow: 0 14px 28px rgba(0,0,0,0.25), 0 10px 10px rgba(0,0,0,0.22); /* Added shadow */
            padding: 50px 35px;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        form * {
            font-family: 'Montserrat', sans-serif; /* Changed to match the Password Reset page */
            color: #333; /* Changed to match the Password Reset page */
            letter-spacing: 0.5px;
            outline: none;
            border: none;
        }

        h4 {
            font-size: 1.25rem;
            margin-bottom: 10px;
            text-align: center;
            font-weight: 500;
        }

        h2 {
            font-size: 1.5rem;
            text-align: center;
            color: rgb(16, 37, 60)
        }

        input {
            display: block;
            height: 50px;
            width: 100%;
            background-color: #eee; /* Changed to match the Password Reset page */
            border-radius: 3px;
            padding: 0 10px;
            margin-top: 20px;
            font-size: 1rem;
            font-weight: 300;
            transition: background-color 0.3s ease;
        }

        input:focus {
            background-color: rgba(255, 255, 255, 0.1); /* Kept original focus style */
        }

        /* Submit Button Styling */
        input[type="submit"] {
            margin-top: 20px;
            width: 100%;
            background: rgb(16, 37, 60); /* Changed to match the Password Reset page */
            color: #FFFFFF; /* Changed to match the Password Reset page */
            padding: 15px 0;
            font-size: 1.125rem;
            font-weight: bold; /* Changed to match the Password Reset page */
            border-radius: 20px; /* Changed to match the Password Reset page */
            cursor: pointer;
            transition: all 0.4s ease;
        }

        input[type="submit"]:hover {
            background: linear-gradient(109.6deg, rgb(36, 45, 57) 11.2%, rgb(16, 37, 60) 51.2%, rgb(0, 0, 0) 98.6%); /* Kept original hover style */
            transform: scale(1.05); /* Added hover effect */
        }

        /* Error Message Styling */
        .error-message {
            margin-top: 10px;
            color: red;
            text-align: center;
            font-size: 0.875rem;
            visibility: hidden;
            height: auto;
            opacity: 0;
            transition: opacity 0.3s ease;
        }

        .error-message.active {
            visibility: visible;
            opacity: 1;
        }

        /* Media Queries for responsiveness */
        @media (max-width: 1024px) {
            form {
                padding: 40px 30px;
            }

            h2 {
                font-size: 1.5rem;
            }

            input {
                height: 45px;
                font-size: 0.875rem;
            }

            input[type="submit"] {
                padding: 12px 0;
                font-size: 1rem;
            }
        }

        @media (max-width: 768px) {
            form {
                padding: 35px 25px;
            }

            h2 {
                font-size: 1.4rem;
            }

            input {
                height: 40px;
                font-size: 0.875rem;
            }

            input[type="submit"] {
                padding: 12px 0;
                font-size: 0.875rem;
            }
        }

        @media (max-width: 480px) {
            form {
                padding: 30px 20px;
            }

            h2 {
                font-size: 1.25rem;
            }

            input {
                height: 40px;
                font-size: 0.75rem;
            }

            input[type="submit"] {
                padding: 10px 0;
                font-size: 0.75rem;
            }

            .error-message {
                font-size: 0.75rem;
            }
        }

    </style>
</head>
<body>
    <h4>OTP has been sent to</h4>
    <h2><%= Email %></h2>
    <form action="/users/verify-otp" method="post">
        <h3>Enter OTP</h3>
        <input type="text" name="otpInput" placeholder="Enter OTP" required>

        <!-- Error message -->
        <div class="error-message <%= error ? 'active' : '' %>"><%= error || '' %></div>

        <input type="submit" value="Submit">
    </form>
</body>

</html>